services:
  postgres:
    image: postgres:18
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: boredgames
      POSTGRES_PASSWORD: boredgames
      POSTGRES_DB: boredgames
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: